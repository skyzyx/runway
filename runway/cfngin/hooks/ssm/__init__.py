"""AWS SSM hooks.

This package provides hooks for managing SSM Parameter Store entries whose
lifecycle must be coordinated with CloudFormation stack deployments, even
though the parameters themselves are not managed by CloudFormation.
"""
