"""Empty init for python import traversal.

This package exists to group all hooks that coordinate Cognito-backed
authentication at CloudFront's edge layer, keeping the auth orchestration
separate from the general static-site deployment hooks.
"""
